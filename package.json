{
  "name": "edtunnel",
  "version": "1.0.0",
  "description": "VLESS tunnel for Cloudflare Pages",
  "main": "_worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "build": "mkdir -p dist && touch dist/_worker.js",
    "dev": "wrangler dev --remote"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "3.2.0"
  }
}
